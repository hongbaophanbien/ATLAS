# ATLAS X 1.8.6 — Mobile Add Ticker

- Adds a mobile-friendly `Thêm mã nhanh` field.
- Enter `SPCX` and tap `ADD TICKER`.
- Supports multiple tickers: `AMD, AVGO, ALAB`.
- Updates the main Watchlist immediately.
- Rebuilds Watch Engine choices from the edited Watchlist.
- Attempts Supabase synchronization when write access is available.

Minimum update:
- Replace `app.py`

Commit:
`ATLAS X 1.8.6: add mobile quick ticker workflow`
