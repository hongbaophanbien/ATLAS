# ATLAS X 1.8.4 — Mobile Sticky Ticker

Mobile layout update:

- Only `Ticker` remains sticky.
- `Action`, `Conviction`, and `Signal` now move horizontally.
- Reduces the width of the first columns on iPhone.
- Makes table padding and font size more compact on screens under 768px.
- Applies to Top Opportunities, LIVE BOT, FAST PICKS, and shared tables.

Minimum update:

- Replace `app.py`

Commit:

`ATLAS X 1.8.4: keep only ticker sticky on mobile`
